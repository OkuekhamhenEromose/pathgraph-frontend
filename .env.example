# Base URL of the PathGraph FastAPI backend (no trailing slash).
# The backend's CORS is restricted to FRONTEND_URL in its own .env —
# make sure that matches wherever this app is actually served from.
VITE_API_URL=http://localhost:8000
